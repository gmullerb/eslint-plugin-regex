plugins {
  id 'com.moowork.node' version '1.2.0'
}

repositories {
  jcenter()
}

// Plugins settings
///////////////////

node {
  version = NODE_VERSION
  download = true
}

// Default task
///////////////

defaultTasks 'npmInstall', 'npm_run_check'
